function main() {
    ok = 1;

    if (1 && 1)  { } else { ok = 0; }
    if (1 && 0)  { ok = 0; }
    if (0 && 1)  { ok = 0; }
    if (0 || 1)  { } else { ok = 0; }
    if (1 || 0)  { } else { ok = 0; }
    if (0 || 0)  { ok = 0; }
    if (!0)      { } else { ok = 0; }
    if (!1)      { ok = 0; }
    if (!42)     { ok = 0; }
    if (!!1)     { } else { ok = 0; }

    if (ok) { return 42; }
    return 0;
}
